function ChartPlaceholderCard({ title }) {
  return (
    <article className="card">
      <h3>{title}</h3>
      <div className="chart-placeholder">
        <span>Data visualization placeholder</span>
      </div>
    </article>
  );
}

export default ChartPlaceholderCard;
